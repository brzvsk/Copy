import SwiftUI

struct ShelfPlaceholderView: View {
    var body: some View {
        Text("Shelf coming together")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(.regularMaterial)
    }
}
